import currentBlockEntity from './currentBlockEntity';
import gameState from './gameState';
import groupEntity from './groupEntity';
import scene from './scene';

export default {
    'currentBlockEntity': currentBlockEntity,
    'gameState': gameState,
    'groupEntity': groupEntity,
    'scene': scene
};
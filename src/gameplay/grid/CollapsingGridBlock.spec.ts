describe(`Collapsing grid block`, () => {
    it(`is testable`, () => {
        expect(1).toBeTruthy();
    });
    // test(`cells with the same type in the same row can be removed and cause the other taken cells to collapse`, () => {});
});